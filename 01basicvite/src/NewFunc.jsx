function NewFunc() {
    return (
        <h1>Hello React</h1>
    )
}

export default NewFunc

// Notice that <NewFunc /> starts with a capital letter.
//  That’s how you know it’s a React component. React 
// component names must always start with a capital letter, 
// while HTML tags must be lowercase.

